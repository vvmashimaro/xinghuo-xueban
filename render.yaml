services:
  - type: web
    name: xinghuo-api
    runtime: node
    plan: free
    rootDir: _platform/server
    buildCommand: npm install
    startCommand: node src/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "20"
